server:
  port: 8888

spring:
  application:
    name: CONFIG-SERVER

  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: file:///D:/STS/config-repo