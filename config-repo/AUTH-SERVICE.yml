server:
  port: 8083

spring:
  application:
    name: AUTH-SERVICE

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true

# 🔥 IMPORTANT (same secret must match Gateway)
jwt:
  secret: mysecretkeymysecretkeymysecretkey123456
  expiration: 3600000